[*Test Case #2*]

function add (number1, number2) 
{
    return number1 + number2;
}

#
    int test0, test1;
    bool trueOrFalse;
    real test2;
#

get (test0, test1);
if (test0 => test1)
{
    return add (test0, test1);
}
fi